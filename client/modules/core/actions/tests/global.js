const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../global';

describe('core.actions.global', () => {
  it('should do something');
});
