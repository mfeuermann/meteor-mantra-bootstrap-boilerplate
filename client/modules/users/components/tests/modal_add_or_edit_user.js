const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import ModalAddOrEditUser from '../modal_add_or_edit_user';

describe('users.components.modal_add_or_edit_user', () => {
  it('should do something');
});
